name: Build
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    steps:
      - uses: actions/checkout@master
      - uses: r-lib/actions/setup-r@v1
        with:
          r-version: '3.5.3'
      - name: Install dependencies
        run: |
          Rscript -e 'install.packages(c("devtools"))' -e 'devtools::install_github("nstrayer/datadrivencv")'
